import { CSSProperties } from 'react';

export default function createStyles() {
  return {
    gameScreen__ctnr: <CSSProperties> {
      width: '100%',
      height: '100%',
      position: 'relative'
    }
  };
}
