const gameScreen__level: Object = {
  position: "absolute",
  top: 0,
  left: 0,
  right: 0,
  bottom: 0,
  width: "100%",
  height: "100%"
};

export default function createStyles(vars: Object): Object {
  return {
    gameScreen__ctnr: {
      width: "100%",
      height: "100%",
      position: "relative"
    }
  };
}
